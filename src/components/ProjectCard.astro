---
const projectTitle = Astro.props.projectTitle;
const projectDescription = Astro.props.projectDescription;
---

<article style={{
  backgroundImage: 'radial-gradient(circle, #6c6c6c1d 0%, transparent 100%)'
}}
class="border border-zinc-400 bg-zinc-600 bg-opacity-50  p-8 rounded-lg flex flex-col-reverse md:flex-row justify-between mb-7 shadow-xl
hover:bg-zinc-700 hover:border-zinc-700 transition dark:bg-zinc-800 dark:border-zinc-800 dark:hover:bg-zinc-900 dark:hover:border-zinc-900
">
  <div>
    <h3 class="text-xl font-semibold inline-flex items-center gap-2 mb-2 text-zinc-100 ">{projectTitle}</h3>
    <p class="text-zinc-100 dark:text-zinc-400 text-sm text-pretty pr-2 mb-4">{projectDescription}</p>
    <div class="flex items-center gap-3">
      <slot/>
    </div>
  </div>
  <slot name="image" />
</article>